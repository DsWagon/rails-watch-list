  <div class="container-fluid py-5" >
    <nav class="navbar nav-background ">
    <span class="navbar-brand mb-0 h1">List of movies category</span>
    <%= link_to new_list_path, class: "btn btn-primary" do %>
    New list
    <% end %>
  </div>
  </nav>

<div class="d-flex justify-content-center">
  <% @lists.each do |list| %>
    <% list.bookmarks.each do |bookmark| %>
      <div class="card mx-2" style="width: 18rem;">
        <%= image_tag bookmark.movie.poster_url, width: 100, class:"card-img-top" %>
          <div class="card-body">
            <h5 class="card-title text-center"><%= link_to list.name, list_path(list), class:"btn btn-primary" %>
          </div>
      </div>
    <% end %>
  <% end %>
</div>



<% if false %>
  <%= cl_image_tag("pawzm0ueovudugee71wp",
        width: 400, height: 300, crop: :fill) %>
<% end %>


<hr>
<div class="footer">
  <div class="footer-links">
    <a href="#"><i class="fab fa-github"></i></a>
    <a href="#"><i class="fab fa-instagram"></i></a>
    <a href="#"><i class="fab fa-facebook"></i></a>
    <a href="#"><i class="fab fa-twitter"></i></a>
    <a href="#"><i class="fab fa-linkedin"></i></a>
  </div>
  <div class="footer-copyright">
    This footer is made with <i class="fas fa-heart"></i> from David
  </div>
</div>
